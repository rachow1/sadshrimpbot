module.exports = {
    name: 'almighty',
    execute(isAlmighty, args) {

        if (isAlmighty) response = '\*\*W H O S U M M O N E D M E ?\*\*';
        else response = 'hi im back did i miss anything';

        return response;
    }
}